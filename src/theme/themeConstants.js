
export const fontFamily = {
    base: "Nunito Sans, Helvetica Neue, Helvetica, Arial, sans-serif",
    secondary: 'Scope One, Helvetica, Arial',
  };

const colors = {
    black:'#000'
}

// color palette
export const theme = {
    colors,
    fontFamily
}